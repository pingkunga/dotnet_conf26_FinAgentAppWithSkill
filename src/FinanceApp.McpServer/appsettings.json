{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "// Urls": "Dev default port, overridable via --urls or ASPNETCORE_URLS (McpServerProcessTests passes --urls explicitly for a fresh port per test run).",
  "Urls": "http://localhost:5299",
  "ConnectionStrings": {
    "Finance": "Host=localhost;Database=financeapp;Username=financeapp;Password=changeme"
  },
  "Mcp": {
    "// SigningKey": "DEV-ONLY placeholder, shared with FinanceApp.Web's appsettings.json (Mcp:SigningKey). Production needs a real secret via user-secrets/env var, not a checked-in value — same posture as the AI provider API keys elsewhere in this project.",
    "SigningKey": "dev-only-shared-secret-CHANGE-ME-32-bytes-min"
  }
}
